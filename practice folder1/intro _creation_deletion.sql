-- for logging into the database or the mysql server account from command prompt you should use the following command.
-- command:
    --  mysql -u (root/account user name) -p then enter
    --  and it will ask for password enter the account password



-- creating databases

create database nexus;

-- for seeing the availabe databases

show databases;

-- working with and using the database is like as follows once you get into the database then you can create tables

use nexus;

--for deleting the database

drop DATABASE nexus;